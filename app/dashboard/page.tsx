import Button from '@/components/ui/Button'
import { Ghost } from 'lucide-react'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <Button size='lg' variant='default'>Hello Button</Button>
}

export default page